files:
  "/etc/nginx/conf.d/websocket_timeout.conf":
    mode: "000644"
    owner: root
    group: root
    content: |
      # 웹소켓 타임아웃 설정
      proxy_read_timeout 180;
      proxy_send_timeout 180;
      keepalive_timeout 180;